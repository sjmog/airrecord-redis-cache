RSpec.describe Airrecord::Redis::Cache do
  it "has a version number" do
    expect(Airrecord::Redis::Cache::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
